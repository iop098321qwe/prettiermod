#!/usr/bin/env bash

function pretty() {
  # write a pretty command launcher like `pretty html` or `pretty json` to launch prettier via npx on specific file types. 
  # Can use `pretty web` to launch prettier on all web related file types (html, css, js, json, etc.)
  pass
}
